# Bug Triage Workflow with Microsoft Agent Framework

A compact Python project skeleton for a bug triage workflow.

- `src/` contains core workflow modules.
- `tests/` contains unit test placeholders.
